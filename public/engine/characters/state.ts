namespace Lich {
    export enum CharacterState {
        WALKR,
        WALKL,
        IDLE,
        BREATH,
        JUMP,
        JUMPR,
        JUMPL,
        MIDAIR,
        FALL,
        DIE,
        DEAD,
        TELEPORT,
    }
}